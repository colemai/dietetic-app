<div class="container">

<%= simple_form_for(@measurement) do |f| %>
  
  <% if @measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
      <% @measurement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :height,  label: "Height in meters", input_html: {step: '0.1', min: '0.1'}  %>
  <%= f.input :weight, label: "Weight in kilograms" %>
  <%= f.input :pal, input_html: {step: '0.1', min: '1.0'} %>
  <%= f.input :gender, collection: ["Male","Female"] %>
  <%= f.input :age %>
  <%= f.button :submit %>
<% end %>

</div>